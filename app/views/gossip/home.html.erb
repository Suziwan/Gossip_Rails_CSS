<div class="container-fluid">
  <br>
  <h4>Welcome to the gossip platform !</h4><br>
    <div class="row">
      <% @gossips.each do |gossip| %>
        <div class="card text-center col-sm-3 border-info mb-3" style="width: 18rem;">
          <div class="card-body">
            <h5 class="card-title">Gossip nº <%= gossip.id %></br></h5>
            <p class="card-text">
            Author : <%= User.find(gossip.user_id.to_i).first_name %> <%= User.find(gossip.user_id.to_i).last_name %></br>
            Title : <%= gossip.title %> <br><br>
            <a href="/home/<%= gossip.id %>/" class="btn btn-info" role="button">Learn more</a>
          </div>
        </div>
      <% end %>
    </div>
</div>
